knp_gaufrette:
    adapters:
        foo_local:
            local:
                directory:  %kernel.root_dir%
                create:     true

    filesystems:
        foo:
            adapter:        foo_local
            alias:          foo_filesystem
